import { EmailCheckDirective } from './email-check.directive';

describe('EmailCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailCheckDirective();
    expect(directive).toBeTruthy();
  });
});
