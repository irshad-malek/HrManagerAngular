import { EmployeeEmailExistDirective } from './employee-email-exist.directive';

describe('EmployeeEmailExistDirective', () => {
  it('should create an instance', () => {
    const directive = new EmployeeEmailExistDirective();
    expect(directive).toBeTruthy();
  });
});
